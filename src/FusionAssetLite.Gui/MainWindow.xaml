<Window x:Class="FusionAssetLite.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FusionAssetLite"
        Width="1000"
        Height="700"
        MinWidth="860"
        MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#EFF2EC"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        AllowDrop="True"
        DragOver="Window_DragOver"
        Drop="Window_Drop"
        Closing="Window_Closing">
    <Window.Resources>
        <SolidColorBrush x:Key="InkBrush" Color="#1D211F" />
        <SolidColorBrush x:Key="MutedBrush" Color="#68716C" />
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="LineBrush" Color="#DCE1DA" />
        <SolidColorBrush x:Key="AccentBrush" Color="#0E7C66" />
        <SolidColorBrush x:Key="AccentDarkBrush" Color="#0A5F4E" />
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#E1F2ED" />
        <SolidColorBrush x:Key="FieldBrush" Color="#F8FAF7" />
        <SolidColorBrush x:Key="WarnBrush" Color="#B45309" />
        <SolidColorBrush x:Key="StatusIdleBrush" Color="#9CA3AF" />
        <SolidColorBrush x:Key="StatusSuccessBrush" Color="#0E7C66" />
        <SolidColorBrush x:Key="StatusWarningBrush" Color="#B45309" />
        <SolidColorBrush x:Key="StatusErrorBrush" Color="#DC2626" />

        <DropShadowEffect x:Key="CardShadow" Color="#1D211F" Opacity="0.08" BlurRadius="20" ShadowDepth="3" Direction="270" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="TextWrapping" Value="NoWrap" />
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="1,0,0,6" />
        </Style>

        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,3,0,0" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{StaticResource FieldBrush}" />
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="CaretBrush" Value="{StaticResource InkBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Focusable="False" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#B8C2BA" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="Padding" Value="16,7" />
            <Setter Property="BorderBrush" Value="#C4CCC5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="7" />
                            <Border x:Name="Overlay"
                                    Background="#1D211F"
                                    Opacity="0"
                                    CornerRadius="7" />
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Overlay" Property="Opacity" Value="0.06" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Overlay" Property="Opacity" Value="0.14" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                                <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentDarkBrush}" />
            <Setter Property="Foreground" Value="#FFFFFF" />
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FFF7ED" />
            <Setter Property="BorderBrush" Value="#FDBA74" />
            <Setter Property="Foreground" Value="{StaticResource WarnBrush}" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="0,0,16,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="Box"
                                    Width="17" Height="17"
                                    CornerRadius="5"
                                    BorderThickness="1.2"
                                    BorderBrush="#AEB8B0"
                                    Background="#FFFFFF"
                                    VerticalAlignment="Center">
                                <Path x:Name="Check"
                                      Data="M 3,8 L 7,12 L 14,4"
                                      Stroke="#FFFFFF"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      Stretch="Uniform"
                                      Margin="2.5"
                                      Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Margin="7,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="Background" Value="#E4EAE4" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border x:Name="PART_Track"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4" />
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="4"
                                    HorizontalAlignment="Left" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Effect" Value="{StaticResource CardShadow}" />
        </Style>

        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="12,9" />
        </Style>
    </Window.Resources>

    <Grid Margin="20,16,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="34" Height="34" CornerRadius="9" VerticalAlignment="Center">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#13937A" Offset="0" />
                            <GradientStop Color="#0A5F4E" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="F" Foreground="#FFFFFF" FontSize="17" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="FusionAssetLite" FontSize="16" FontWeight="SemiBold" />
                    <TextBlock Text="Extract images, sounds, packed data and shaders from game packages"
                               Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,1,0,0" />
                </StackPanel>
            </StackPanel>
            <TextBlock Text="Drop a package anywhere in this window"
                       Foreground="{StaticResource MutedBrush}" FontSize="11"
                       HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left panel: setup -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="INPUT PACKAGE" Style="{StaticResource FieldLabel}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="InputPathTextBox" Grid.Column="0" MinHeight="38" IsReadOnly="True" TextChanged="InputPathTextBox_TextChanged" />
                            <Button Grid.Column="2" Content="Browse" Click="BrowseInput_Click" />
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="0,16,0,0">
                        <TextBlock Text="OUTPUT FOLDER" Style="{StaticResource FieldLabel}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="OutputPathTextBox" Grid.Column="0" MinHeight="38" TextChanged="OutputPathTextBox_TextChanged" />
                            <Button Grid.Column="2" Content="Browse" Click="BrowseOutput_Click" />
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="0,20,0,0">
                        <TextBlock Text="ASSETS" Style="{StaticResource FieldLabel}" />
                        <WrapPanel>
                            <CheckBox x:Name="ImagesCheckBox" Content="Images" IsChecked="True" />
                            <CheckBox x:Name="SoundsCheckBox" Content="Sounds" IsChecked="True" />
                            <CheckBox x:Name="PackedDataCheckBox" Content="Packed Data" IsChecked="True" />
                            <CheckBox x:Name="ShadersCheckBox" Content="Shaders" IsChecked="True" />
                        </WrapPanel>
                    </StackPanel>

                    <Border Grid.Row="3" Margin="0,14,0,18" Background="{StaticResource FieldBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                        <StackPanel>
                            <TextBlock Text="OUTPUT" Style="{StaticResource FieldLabel}" />
                            <TextBlock x:Name="OutputDetailTextBlock" Text="No run yet" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" FontSize="12" />
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button x:Name="ExtractButton" Grid.Column="0" Content="Extract" MinHeight="40" FontSize="14" Style="{StaticResource PrimaryButton}" Click="Extract_Click" />
                        <Button x:Name="CancelButton" Grid.Column="2" Content="Cancel" MinHeight="40" Style="{StaticResource DangerButton}" IsEnabled="False" Click="Cancel_Click" />
                    </Grid>
                </Grid>
            </Border>

            <!-- Right column: status + log -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource Card}" Padding="18,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="14" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock x:Name="StageTextBlock" Text="Waiting" FontSize="19" FontWeight="SemiBold" />
                            <TextBlock x:Name="RunDetailTextBlock" Text="Choose a package and output folder." Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,3,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top">
                            <Border Background="{StaticResource FieldBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="14" Padding="11,7" Margin="0,0,8,0" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="StatusDot" Width="8" Height="8" Fill="{StaticResource StatusIdleBrush}" Margin="0,0,7,0" VerticalAlignment="Center" />
                                    <TextBlock x:Name="StatusTextBlock" Text="Idle" FontSize="12" FontWeight="SemiBold" />
                                </StackPanel>
                            </Border>
                            <Button x:Name="OpenOutputButton" Content="Open Output" Margin="0,0,8,0" IsEnabled="False" Click="OpenOutput_Click" />
                            <Button x:Name="CopyLogButton" Content="Copy Log" IsEnabled="False" Click="CopyLog_Click" />
                        </StackPanel>

                        <ProgressBar x:Name="ExtractionProgressBar" Grid.Row="2" Grid.ColumnSpan="3" Height="8" Minimum="0" Maximum="100" Value="0" />
                    </Grid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="16,14,16,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource StatCard}" Background="{StaticResource AccentSoftBrush}" BorderBrush="#BCDFD5" Margin="0,0,8,0">
                                <StackPanel>
                                    <TextBlock Text="IMAGES" Style="{StaticResource StatLabel}" />
                                    <TextBlock x:Name="ImagesSummaryTextBlock" Text="–" Style="{StaticResource StatValue}" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource StatCard}" Background="#F1F3F5" BorderBrush="#DBE0E6" Margin="0,0,8,0">
                                <StackPanel>
                                    <TextBlock Text="SOUNDS" Style="{StaticResource StatLabel}" />
                                    <TextBlock x:Name="SoundsSummaryTextBlock" Text="–" Style="{StaticResource StatValue}" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource StatCard}" Background="#FFF6EB" BorderBrush="#F6DDBC" Margin="0,0,8,0">
                                <StackPanel>
                                    <TextBlock Text="PACKED" Style="{StaticResource StatLabel}" />
                                    <TextBlock x:Name="PackedSummaryTextBlock" Text="–" Style="{StaticResource StatValue}" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="3" Style="{StaticResource StatCard}" Background="#EEF1FD" BorderBrush="#D4DBF6">
                                <StackPanel>
                                    <TextBlock Text="SHADERS" Style="{StaticResource StatLabel}" />
                                    <TextBlock x:Name="ShadersSummaryTextBlock" Text="–" Style="{StaticResource StatValue}" />
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Grid.Row="1" Margin="16,0,16,16" Background="#131915" CornerRadius="8" Padding="2">
                            <TextBox x:Name="LogTextBox"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"
                                     TextWrapping="NoWrap"
                                     Padding="10,8"
                                     FontFamily="Cascadia Mono, Consolas"
                                     FontSize="12"
                                     Background="Transparent"
                                     Foreground="#D8E7DE"
                                     BorderThickness="0"
                                     SelectionBrush="{StaticResource AccentBrush}"
                                     CaretBrush="#D8E7DE" />
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
